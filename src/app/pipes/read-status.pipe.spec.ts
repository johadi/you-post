import { ReadStatusPipe } from './read-status.pipe';

describe('ReadStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
