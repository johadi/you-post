import { SetAddUserButtonDirective } from './set-add-user-button.directive';

describe('SetAddUserButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAddUserButtonDirective();
    expect(directive).toBeTruthy();
  });
});
